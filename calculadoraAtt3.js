// Calculadora de soma feita por função

function soma(x, y){ // função de soma
    var res = Number(x) + Number(y);
    alert("Resultado: "+res)
}

var x = prompt("Digite o primeiro valor:");
var y = prompt("Digite o segundo valor:"); 

soma(x, y);